1、函数用了settimeout，这个函数是指定毫秒数后调用函数或者是计算表达式的，而settimeout设置为0s，你以为他会马上执行？其实他被js放在队列中了，要等别人执行完了，他才会执行。所以如果你想让后面的语句先执行在执行本身的话就用settimeout(0)吧。
回调时，被回调的函数会被放在event loop里，等待线程里的任务执行完后才执行event loop里的代码。
2、在ECMAScript6 中,promise的状态有Pending，Resolved，Rejected。
3、把正确的验证码文本放在了客户端，这是违背了验证码的初衷的。爬虫或者是恶意程序依旧可以通过各种手段获取你嵌入在html文本或者保存在cookie中的正确验证码文本，模拟表单提交来达到攻击的目的。
正确的实现方式，发送到客户端的只有图片形式的验证码，服务器端保存cookie对应的图形验证码的正确文本。客户端表单提交时到服务器端验证。
4、加号优先级高于 三目运算。低于括号。 所以括号中无论真假 加上前边的字符串都为 TRUE 三目运算为TRUE是 输出 define。
5、angularjs1中control间通信最好使用什么方式？  广播
6、设置RadioButton控件的 GroupName 属性控制用户在多个单选按钮中只能选中一个。
7、下面哪一个标符可以产生一个水平线  <HR>
8、AngularJS1元素过滤的正确写法是哪一个  x in list | filter:query
9、angularjs1中定义服务哪些方法是正确的？$provide、factory、service
10、有5种primitive type，分别是Undefined、Null、Boolean、Number 和 String。
11、正确的标签有 <br />  <hr />  <img />
12、div>p的意思是说：选择所有父元素为div的p元素，如果一个p元素的父元素不是div，那么这个p元素是不会被选择的！！！
13、 <canvas> 标签定义图形，比如图表和其他图像。<canvas> 标签只是图形容器，您必须使用脚本来绘制图形。 <audio> 标签定义声音，比如音乐或其他音频流。<article>标签定义外部的内容。比如来自一个外部的新闻提供者的一篇新的文章，或者来自 blog 的文本，或者是来自论坛的文本。亦或是来自其他外部源内容。 <menu> 标签定义命令的列表或菜单。<menu> 标签用于上下文菜单、工具栏以及用于列出表单控件和命令。command 元素表示用户能够调用的命令。<command> 标签可以定义命令按钮，比如单选按钮、复选框或按钮。只有当 command 元素位于 menu 元素内时，该元素才是可见的。否则不会显示这个元素，但是可以用它规定键盘快捷键。 
14、不可继承的属性太多了不要背，记住可以继承的属性有哪些就行了。可以继承的属性很少，只有颜色，文字，字体间距行高对齐方式，和列表的样式可以继承。